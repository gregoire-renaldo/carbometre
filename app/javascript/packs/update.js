// display the values before user input add event listener ke
// to display score on edit footprint, before user input & event lister








const defaultAutre = (Number(defaultAspirateur) + Number(defaultCarotte)).toFixed(2)
totalAutre.innerHTML = defaultAutre


totalBilan.innerHTML = (Number(defaultVoiture) + Number(defaultTransport)
+ Number(defaultEnergie) + Number(defaultAlim) + defaultAutre;

